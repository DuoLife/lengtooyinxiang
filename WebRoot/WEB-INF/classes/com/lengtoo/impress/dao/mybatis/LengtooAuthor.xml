<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
  <mapper namespace="LengtooEmoji">
  <!-- app -->
  	<select id="selectById" resultType="LengtooEmoji">
  		select * from LengtooEmoji where packageid = ${packageid} and isshow = 1 order by createdate desc;
  	</select>
  </mapper>