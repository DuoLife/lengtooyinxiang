<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="LengtooWallpaper">
	<insert id="insertOne" parameterType="LengtooWallpaper">
		<selectKey resultType="int" keyProperty="wallpaperid">
			select last_insert_id();
		</selectKey>
		insert into lengtoo_bizhi (big_imgurl, small_imgurl, width, height, default_text, upload_ip, createdate, small_width, small_height) 
		values(#{big_imgurl}, #{small_imgurl}, #{width}, #{height}, #{default_text}, #{upload_ip}, #{createdate}, 
 		 #{small_width}, #{small_height});
	</insert>
	<select id="selectAll" parameterType="Map" resultType="LengtooWallpaper">
		select * from lengtoo_bizhi where createdate &lt; #{date} order by createdate desc limit #{limit}
	</select>
</mapper>
